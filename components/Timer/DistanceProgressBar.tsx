import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const DistanceProgressBar = () => {
  return (
    <View>
      <Text>DistanceProgressBar</Text>
    </View>
  )
}

export default DistanceProgressBar

const styles = StyleSheet.create({})