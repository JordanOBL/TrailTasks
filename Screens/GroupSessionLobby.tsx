import { StyleSheet, Text, View } from "react-native";

import React from "react";

const GroupSessionLobby = () => {
  return (
    <View>
      <Text>GroupSessionLobby</Text>
    </View>
  );
};

export default GroupSessionLobby;

const styles = StyleSheet.create({});
